// Given two sorted linked lists, merge them so that the resulting linked list is also sorted. 
// Consider two sorted linked lists and the merged list below them as an example
// LinkList = 4, 8, 15, 19   LinkList2 = 7, 9 , 10, 16

class MergeTwoLinkList {

    public static void main (String ... args) {
        String [] test = "testing example".split(",");
        // System.out.println(test[0].substring(0, 1).toUpperCase() + test[0].substring(1).toLowerCase());
        System.out.println(test[0].trim());
	}
    



}